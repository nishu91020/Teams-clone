import React from 'react';
import Header from '../../Components/Header';
const VideoChat = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default VideoChat;
