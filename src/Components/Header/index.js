//header for all the screens.

import React from 'react';
import './styles.css';
const Header = props => {
    return (
        <div className="HeaderContainer">
            <p>Meet Up</p>
        </div>
    );
};

export default Header;
